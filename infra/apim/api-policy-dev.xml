<!--
  Developer-tier APIM variant — omits set-header/rate-limit expressions that
  throw on wfprod-apim-dev when output-token-variable jwt Claims access fails.
-->
<policies>
  <inbound>
    <base />
    <set-header name="X-WriterFlow-Origin" exists-action="override">
      <value>{{writerflow-origin-secret}}</value>
    </set-header>
    <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized" require-scheme="Bearer">
      <openid-config url="{{writerflow-jwks-uri}}" />
      <audiences>
        <audience>https://apiwriterflow.aviusolutions.com</audience>
      </audiences>
      <issuers>
        <issuer>{{writerflow-issuer}}</issuer>
      </issuers>
      <required-claims>
        <claim name="device_id" match="any" />
      </required-claims>
    </validate-jwt>
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
    <set-header name="Cache-Control" exists-action="override">
      <value>no-store</value>
    </set-header>
  </outbound>
  <on-error>
    <base />
    <set-header name="Content-Type" exists-action="override">
      <value>application/json</value>
    </set-header>
    <set-body>@{
      return new JObject(
        new JProperty("error", new JObject(
          new JProperty("code", context.LastError.Source == "validate-jwt" ? "AUTH_INVALID" : "INTERNAL_ERROR"),
          new JProperty("message", "Request could not be completed.")
        ))
      ).ToString();
    }</set-body>
  </on-error>
</policies>
