{
  "name": "@writerflow/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Outbox dispatch and reconciliation jobs (Stage 5.5). Stage 5.1 scope is the buildable/testable workspace skeleton only.",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@writerflow/shared": "0.1.0",
    "pg": "^8.22.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
